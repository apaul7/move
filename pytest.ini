[pytest]
# Tests import top-level modules (filtering, gene_track) and tests/ has no
# __init__.py, so pytest only puts tests/ on sys.path -- not the repo root.
# Without this, the bare `pytest` that CI and the README use fails collection
# with ModuleNotFoundError, and only `python -m pytest` works.
pythonpath = .
testpaths = tests
